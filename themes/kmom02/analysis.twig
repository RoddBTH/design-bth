{% include 'incl/header.twig' %}

<body>
    {% include 'incl/nav.twig' %}

    <div class="main" role="main">
        <div class ="landing-page">
            {{ content }}

            {% for tech in meta.techs %}
                <a class="box {{ tech.class}}" href="{{ base_url }}/analysis/{{ tech.url|lower }}"> {#|lower is a filter from jinja template#}
                    <h1> {{ tech.name }} </h1>
                </a>
            {% endfor %}
        </div>
    </div>
    
    {% include 'incl/footer.twig' %}
</body>

</html>
